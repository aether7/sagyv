Liquidacion:
    #TODO
        ~OK - Falta KM.
        ~OK - Falta Ultima Boleta.
        ~OK - No se estan sumando los cheques.
        ~OK - No hay data en la respuesta.
        NOK - No se estan sumando "Otros".
        NOK - Venta de garantias.
        NOK - Descarga de vehiculo.
        NOK - Ingreso de voucher lipigas.
        NOK - Ingreso de voucher Transbank.
        NOK - lo elementos adicionales en el detalle de guia salen sin el signo $.
--------NOK - Falta un total de la sumatoria. #Solo como idea.
!!!!!!!!NOK - Retirar Carga del vehiculo [IMPORTANTE] --Preguntar a andrea


# class Voucher(models.Model):
#     tipo_tarjeta = models.ForeignKey(TipoTarjeta, null = True)
#     tipo_cuotas = models.CharField(max_length = 140, null = True)
#     terminal = models.ForeignKey(Terminal)
#     numero_tarjeta = models.IntegerField(null = True)
#     numero_operacion = models.IntegerField(null = True)
#     codigo_autorizacion = models.IntegerField(null = True)
#     numero_cuotas = models.IntegerField(default = 1)
#     liquidacion = models.ForeignKey(Liquidacion)
